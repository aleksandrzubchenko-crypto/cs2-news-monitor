# Primary-source X/Twitter accounts to monitor (provider-agnostic config).
# One handle per line (without @). Lines starting with # are comments.
# The monitor reads all non-comment lines (active list).
#
# RULE: only ACTIVATE a handle after it's verified to resolve on X/Liquipedia.
# Wrong handle = wrong/None data.
# All active handles below verified via Liquipedia Links / official account (2026-07-02).

# ══ ACTIVE (verified) ══
# insiders / aggregators
HLTVorg
cstransfers
RLewisReports
Thorin
ohnePixel
Sheep_Esports
KRL_STREAM
# official
CounterStrike
# orgs  (CS-specific division handles where the org runs multiple games)
natusvincere
TeamVitalityCS
FaZeEsports
G2esports
AstralisCS
TeamLiquidCS
mousesports
Team__Spirit
1mongolz
TeamFalconsGG
AuroraCS2_GG
# tournament orgs
BLASTPremier
ESLCS
IEM
pglesports
StarLadder_CS
# dataminers (LLM marks output as unconfirmed leak)
ThourCS2
gabefollower
# anti-cheat / stats
AntiCheatPD
leetify
# players (stars)
s1mpleO
zywoo
NiKoCS_
donk1337
FLCm0NESY
# prospects
XKacpersky

# ══ NOT ACTIVATED — needs a decision or could not verify ══
# csfloatcom     — RESOLVES, but CSFloat is a CS2 skin MARKETPLACE = competitor.
#                  Conflicts with the bot's own "no competing skin/case brands" guardrail. Your call.
# pricempire     — could not confirm the exact X handle from search; verify before activating
# FlankEsports   — ambiguous: @FlankEsports vs @Flank_Esport; could not disambiguate the CS outlet
# dexerto        — only a general multi-game @Dexerto; no CS-specific handle -> would add noise
# r0se           — B8 Academy prospect; no X handle found on Liquipedia/HLTV
#
# INTERVIEWS / QUOTES candidates (player quotes, stage/post-match) — verify handles, then activate:
#   BetBoomTeam        — tournament broadcast + interview clips (verified brand)
#   OZ_moses           — stage interviewer/analyst (Jason "moses" O'Toole) — verify
#   HenryG             — caster/interviewer (Henry Greer) — verify
#   OnFireAnders       — caster/analyst (Anders Blume) — verify
#   cs2proplayers      — clip aggregator (TikTok/X) — verify
#   (also: esports.gg, Dexerto CS — quote write-ups; site-level, no clean CS-only X handle)
#
# FACEIT candidates (map pool / seasons / FPL / rankings / bans) — verify, then activate:
#   FACEITcs           — official FACEIT CS2 (news, map pool, seasons) — appears verified
#   (rankings/leaderboards: faceit.com/cs2/rank + faceittracker.net — site-level, not X)
#
# NOTE: KRL_STREAM = insider Sebastien Perez (activated above). NOT the Brazilian player KRL
#       on /counterstrike/KRL -- different account, do not swap.
#       Vitality/FaZe/Astralis/Liquid switched to CS-specific division handles (2026-07-02).
#
# Process: pull exact official handles from Liquipedia team/player pages, verify they
#          resolve, then activate (move into the ACTIVE list).
